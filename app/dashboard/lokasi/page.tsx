import Lokasi from "@/components/page/Lokasi";
export const metadata = {
    title: 'Lokasi Kantor',
    description: 'lokasi kantor ',
  };

const LokasiPage = () => {
  return <Lokasi />;
};

export default LokasiPage;