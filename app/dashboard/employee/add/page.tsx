import FormAddEmployee from "@/components/page/FormAddEmployee";
export const metadata = {
    title: 'Added Employees',
    description: 'Tambah Data Karyawan',
  };

const AddEmployeePage = () => {
  return <FormAddEmployee />;
};

export default AddEmployeePage;